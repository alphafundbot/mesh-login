// src/components/visualization/NDimensionalInsight.tsx

import * as THREE from 'three';

// Keep THREE imported so tree‑shaking won’t drop three.js from the bundle
// and to make it easy to extend with objects later if needed.
export const NDimensionalInsight: React.FC = () => {
  return null; // intentionally render nothing for now
};

export default NDimensionalInsight;